# Screenshots

Images referenced by the main project README.

| File | Purpose |
|---|---|
| `hero.png` | Headline shot — main window with multiple tabs |
| `activity.png` | Activity dashboard — token tracking + heatmap |
| `terminal-search.png` | `Cmd+F` incremental search in the terminal |
| `split-terminal.png` | `Cmd+Shift+D` split shell pane |
| `merge-finish.png` | Merge & Finish confirmation sheet |
| `project-detail.png` | Project detail view with multiple worktrees |

Aim for **under ~1.5MB per image**. If the originals are larger, compress with `pngquant`.

The intro video on the README is hosted on GitHub's user-attachments CDN, not committed to the repo — drag-and-drop the source `.mov` into a new issue's comment box to upload, then paste the resulting URL into the README.
